import 'package:flutter/material.dart';

class CurriculoView extends StatelessWidget {
  const CurriculoView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CurriculoView'),
      ),
      body: Center(
        child: Text('CurriculoView'),
      ),
    );
  }
}
