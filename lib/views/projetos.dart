import 'package:flutter/material.dart';

class ProjetosView extends StatelessWidget {
  const ProjetosView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ProjetosView'),
      ),
      body: Center(
        child: Text('ProjetosView'),
      ),
    );
  }
}
